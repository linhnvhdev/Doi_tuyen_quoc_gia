#include <bits/stdc++.h>
#define For(i,a,b) for(int i = a; i <= b; ++i)
#define Forv(i,a) for(int i = 0;i < a.size();i++)
#define maxn 1005
#define ll long long
#define fi first
#define se second
#define INF 1e9+1
#define Bit(x,i) ((x >> i) & 1)
#define pii pair<int,int>
using namespace std;

ll n;
int t,s,e;

int main()
{
    freopen("relays.inp","r",stdin);
    freopen("relays.out","w",stdout);
    cin >> n >> t >> s >> e;
}
